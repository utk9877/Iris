<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTD/PropertyList-1.0.dtd">
<!--
  Hardened-runtime entitlements for the notarized Iris app (Phase 6, ARCHITECTURE §10).
  The bundled PyInstaller sidecar loads unsigned native libraries (onnxruntime + CoreML,
  insightface, opencv) at runtime, so the hardened runtime needs library-validation
  disabled and unsigned-executable-memory allowed. No network entitlement is requested —
  Iris is local-first and runs fully offline.
-->
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
